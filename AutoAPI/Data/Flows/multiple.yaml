flow_id: "001"  # 业务流唯一标识

steps:
  - id: "启动汉诺塔业务"  # 步骤名
    ref: "启动业务"

  - id: "上传数据"
    ref: "上传数据"
    delay_run: 1 # 用启动业务接口必须延迟 2.5 秒执行, 否则又会出现第二个正在运行的 业务, 问题原因未知(用启动模型接口就没有这个问题)
    override:

  - id: "获取对象值"
    ref: "获取对象值"
    delay_run: 15 # 用启动业务接口必须延迟 2.5 秒执行, 否则又会出现第二个正在运行的 业务, 问题原因未知(用启动模型接口就没有这个问题)

  - id: "停止业务"
    ref: "停止任务"

